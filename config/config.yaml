project:
  name: "olist-late-delivery"
  model_version: "1.0.0"

paths:
  paths:
  model: "models/late_delivery_model.joblib"
  preprocessor: "models/preprocessor.joblib"
  feature_list: "models/feature_list.json"

inference:
  threshold: 0.5

mlflow:
  model_name: "olist-late-delivery-model"
  model_version: "1"