project:
  name: "olist-late-delivery"
  model_version: "1.0.0"

paths:
  model: "artifacts/late_delivery_model.joblib"
  preprocessor: "artifacts/preprocessor.joblib"
  feature_list: "artifacts/feature_list.json"

inference:
  threshold: 0.5

mlflow:
  model_name: "olist-late-delivery-model"
  model_version: "1"